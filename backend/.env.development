# ============================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env for local development

# Environment
NODE_ENV=development

# Server Configuration
PORT=5000

# Security - JWT Tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=dev-secret-key-replace-in-production-min-64-chars
JWT_EXPIRES_IN=7d

# Database - Local MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/maptrack

# Email Service - Gmail (for development/testing)
# Note: For Gmail, you need to use an "App Password" if 2FA is enabled
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_FROM=Zero Malaria Zambia <noreply@zeromalaria.zm>

# Frontend URL - Local development
FRONTEND_URL=http://localhost:5173

# Africa's Talking SMS API (Optional)
AT_API_KEY=your_api_key_here
AT_USERNAME=sandbox

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env
# 2. Replace EMAIL_USER and EMAIL_PASS with your Gmail credentials
# 3. Make sure MongoDB is running locally
# 4. Run: npm install
# 5. Run: npm run dev
