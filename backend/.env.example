# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env and fill in your actual values
# For production, see .env.production
# For development, see .env.development

# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGO_URI=mongodb://127.0.0.1:27017/maptrack

# Security - JWT Configuration
# IMPORTANT: Generate a strong secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GENERATE_YOUR_OWN_SECRET_KEY_MIN_64_CHARS
JWT_EXPIRES_IN=7d

# Email Configuration (for verification and password reset)
# For Gmail: Use app-specific password if 2FA is enabled
# For SendGrid (production): Use 'apikey' as EMAIL_USER
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_FROM=Zero Malaria Zambia <noreply@zeromalaria.zm>

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# CORS Origin (for production, set to your frontend domain)
CORS_ORIGIN=http://localhost:5173

# Africa's Talking SMS API (Optional - for SMS notifications)
AT_API_KEY=your_api_key_here
AT_USERNAME=sandbox

# ============================================
# QUICK START
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secret (see command above)
# 3. Update EMAIL_USER and EMAIL_PASS
# 4. Ensure MongoDB is running
# 5. Run: npm install && npm run dev
